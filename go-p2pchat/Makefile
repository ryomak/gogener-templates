NAME="test"
PORT="1111"
USER="test1@192.168.10.1:1000"

run:
	go run chat.go -name $(NAME) -p $(PORT) -user $(USER) 

run-debug:
	go run chat.go -name $(NAME) -p $(PORT) -user $(USER) debug

run-public:
	go run chat.go -name $(NAME) -p $(PORT) -user $(USER) -public
